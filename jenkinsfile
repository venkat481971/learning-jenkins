pipeline {

  agent {
    node { label 'workstation' }
  }

  environment {
    NEWRELIC_API_KEY = credentials('ansible')
    SAMPLE_URL = "google.com"
  }

  stages {

    stage('Foo') {
      steps {
        echo 'Hello world'
        echo 'Bye'
      }
    }